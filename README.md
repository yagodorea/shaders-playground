# shaders-playground

GPU-accelerated particle simulation using Three.js WebGPU and TSL. 2000 particles orbit a planet with gravity, bounce, and friction — all computed on the GPU via instanced arrays.

Requires a browser with WebGPU support (Chrome 113+).

## Run

```
npm install
npm run dev
```

## Deploy

Try at https://yagodorea.github.io/shaders-playground/.
